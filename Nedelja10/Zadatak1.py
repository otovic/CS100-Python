# define the string
string = "Hello World"

if len(string) < 2:
    print(string)
else:
    # get the first two and last two characters from the string
    first_two = string[0:2]
    last_two = string[-2:]

    # put the characters in a new string variable
    new_string = first_two + last_two

    # print the new string
    if(len)
    print(new_string)